---
globs: *.py
---

# Django 开发规范

## 文件结构约定
- 视图逻辑放在 `views.py`
- 模型定义在 `models.py`
- 序列化器在 `serializers.py`
- URL 配置在 `urls.py`

## 编码要求
- 使用 Django REST Framework 进行 API 开发
- 数据库操作优先使用 ORM
- 复杂查询添加中文注释说明业务逻辑
- 视图函数保持简洁，复杂逻辑抽取到 service 层

## 命名规范
- 类名使用 PascalCase
- 函数和变量使用 snake_case
- 常量使用 UPPER_SNAKE_CASE
- 中文注释解释业务含义

## AI/LLM 相关代码
- 模型调用逻辑放在 `models_provider/` 下
- RAG 相关功能在 `knowledge/` 应用中
- 对话处理在 `chat/` 应用中
- 工作流引擎相关代码保持模块化