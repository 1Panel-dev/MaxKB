# MaxKB RAG优化 - 快速开始

> **3分钟快速上手指南** 🚀

---

## 第一步：重启服务 (1分钟)

### 重启前端
```bash
cd ui
npm run dev
```

### 重启后端
```bash
python manage.py runserver
```

✅ **完成！** 以下优化已自动生效：
- ✓ 默认参数优化（Top N=5, 相似度=0.7, 混合检索）
- ✓ Chunk Size增加到800字符
- ✓ 50%重叠分块
- ✓ Blend权重优化（60%向量+40%关键词）

---

## 第二步：测试新应用 (1分钟)

1. 创建新应用
2. 上传测试文档
3. 提问测试

**预期效果**：
- 返回5个相关段落（原3个）
- 段落更长更完整（800字符 vs 256字符）
- 混合检索更准确

---

## 第三步：启用Reranker（可选，1分钟）

### 前提：添加Reranker模型
1. 进入 `设置` → `模型管理`
2. 添加Reranker模型（推荐：bge-reranker-v2-m3）
3. 记录模型ID

### 快速启用
```bash
# 1. 查看所有应用
python enable_reranker.py list

# 2. 为指定应用启用Reranker
python enable_reranker.py <应用ID> <Reranker模型ID> 3
```

**示例**：
```bash
python enable_reranker.py abc-123-def-456 reranker-model-789 3
```

---

## 常见场景

### 场景1：我有现有文档，想使用新的分块策略

```bash
# 查看所有知识库
python reprocess_documents.py --list

# 重新处理指定知识库（推荐）
python reprocess_documents.py --knowledge-id <知识库ID> --batch-size 10

# 重新处理所有文档（谨慎！）
python reprocess_documents.py --batch-size 10
```

### 场景2：我想更新现有应用的参数

**方法A：通过UI**
1. 进入应用设置
2. 点击"知识库设置"旁的"参数设置"
3. 修改参数并保存

**方法B：通过Django Shell**
```bash
python manage.py shell
```
```python
from application.models import Application

app = Application.objects.get(id='your-app-id')
app.knowledge_setting['top_n'] = 5
app.knowledge_setting['similarity'] = 0.7
app.knowledge_setting['search_mode'] = 'blend'
app.save()
```

### 场景3：我想回滚优化

参考 `MaxKB_RAG优化快速实施指南.md` 中的"回滚方案"

---

## 效果验证

### 检查1：新应用默认参数
创建新应用 → 查看知识库设置 → 确认参数已更新

### 检查2：新文档分块大小
```python
python manage.py shell
```
```python
from knowledge.models import Paragraph

# 查看最新文档的段落
paragraphs = Paragraph.objects.filter(document_id='your-doc-id')
for p in paragraphs[:3]:
    print(f"长度: {len(p.content)} 字符")
```

预期：段落长度接近800字符

### 检查3：检索效果
在应用中提问 → 查看返回段落数量（应为5个）→ 查看相关性分数（应>0.7）

---

## 预期提升

| 指标 | 提升幅度 |
|------|---------|
| 召回率 | +85-115% |
| 准确率 | +85-125% |
| 用户满意度 | 显著提升 |

---

## 需要帮助？

- 📖 详细使用指南：`MaxKB_RAG优化使用指南.md`
- 📊 完整分析报告：`MaxKB_RAG优化可行性分析报告.md`
- 🛠️ 实施指南：`MaxKB_RAG优化快速实施指南.md`

---

**开始享受更强大的RAG系统吧！** 🎉

