# MaxKB 本地开发环境快速启动指南

> **项目路径**: `D:/code/v21/MaxKB`  
> **技术栈**: Python 3.11 + Django 5.2 + Vue 3 + PostgreSQL 17 + Redis  
> **更新日期**: 2026-01-20

---

## 📋 目录

1. [系统要求](#1-系统要求)
2. [一键启动（推荐）](#2-一键启动推荐)
3. [手动启动步骤](#3-手动启动步骤)
4. [Windows特殊说明](#4-windows特殊说明)
5. [前端启动](#5-前端启动)
6. [常见问题](#6-常见问题)
7. [开发工具推荐](#7-开发工具推荐)
8. [项目结构说明](#8-项目结构说明)

---

## 1. 系统要求

### 必需软件

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| **Python** | 3.11.x | 后端运行环境 |
| **Node.js** | 18+ | 前端构建工具 |
| **PostgreSQL** | 17.x | 主数据库（需安装pgvector扩展） |
| **Redis** | 6.0+ | 缓存数据库 |
| **Git** | 最新版 | 代码管理 |

### 可选软件

- **Docker Desktop** - 如果使用Docker方式运行
- **PyCharm/VSCode** - 推荐的IDE

---

## 2. 一键启动（推荐）

### 前提条件
- ✅ 已安装 Docker Desktop
- ✅ 已安装 Python 3.11+
- ✅ 已安装项目依赖

### 启动步骤

```bash
# 1. 双击运行启动脚本
start_local_dev.bat

# 脚本会自动完成：
# ✅ 启动PostgreSQL容器（带pgvector扩展）
# ✅ 启动Redis容器
# ✅ 运行数据库迁移
# ✅ 启动MaxKB开发服务器
```

### 停止环境

```bash
# 双击运行停止脚本
stop_local_dev.bat
```

---

## 3. 手动启动步骤

### 步骤1: 启动PostgreSQL（带pgvector）

```bash
# 创建并启动容器
docker run -d --name maxkb-postgres \
  -e POSTGRES_USER=root \
  -e POSTGRES_PASSWORD=Password123@postgres \
  -e POSTGRES_DB=maxkb \
  -p 5432:5432 \
  pgvector/pgvector:pg17

# 验证容器运行
docker ps | findstr maxkb-postgres

# 测试连接
psql -h 127.0.0.1 -p 5432 -U root -d maxkb
# 输入密码: Password123@postgres
```

### 步骤2: 启动Redis

```bash
# 创建并启动容器
docker run -d --name maxkb-redis \
  -p 6379:6379 \
  redis:latest \
  redis-server --requirepass Password123@redis

# 验证容器运行
docker ps | findstr maxkb-redis

# 测试连接
redis-cli -h 127.0.0.1 -p 6379 -a Password123@redis PING
# 应返回: PONG
```

### 步骤3: 配置环境变量

确保 `.env` 文件配置正确（已自动配置）：

```bash
MAXKB_CONFIG_TYPE=ENV
MAXKB_DB_HOST=127.0.0.1
MAXKB_DB_PORT=5432
MAXKB_REDIS_HOST=127.0.0.1
MAXKB_REDIS_PORT=6379
```

### 步骤4: 运行数据库迁移

```bash
python main.py upgrade_db
```

### 步骤5: 安装Python依赖

```bash
# 进入项目目录
cd D:/code/v21/MaxKB

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# 安装uv（快速包管理器）
pip install uv

# 安装项目依赖
python -m uv pip install -r pyproject.toml

# 或使用传统pip（较慢）
pip install -r pyproject.toml
```

### 步骤6: 初始化数据库

```bash
# 进入apps目录
cd apps

# 数据库迁移
python manage.py migrate

# 创建超级用户（可选，系统已有默认admin用户）
# python manage.py createsuperuser
```

**默认管理员账号**:
- 用户名: `admin`
- 密码: `MaxKB@123..`

### 步骤7: 启动开发服务器

```bash
# 回到项目根目录
cd D:/code/v21/MaxKB

# 开发模式启动（自动迁移数据库）
python main.py dev web

# 或启动所有服务（web + celery任务队列）
python main.py start all
```

---

## 4. Windows特殊说明

### ⚠️ 重要提示

MaxKB 原本是为 Linux 设计的，在 Windows 上有以下限制：

1. **不支持 Gunicorn**：Gunicorn 是 Unix 专用的 WSGI 服务器，在 Windows 上无法运行
2. **不支持 daemon 模式**：后台守护进程模式仅在 Unix/Linux 上可用
3. **必须使用开发模式**：在 Windows 上只能使用 Django 的 `runserver` 开发服务器

---

### ✅ Windows 上的正确启动方式

#### 方式1：使用批处理文件（推荐）

双击运行项目根目录下的 `start_local_dev.bat` 文件

#### 方式2：手动启动 Web 服务

```bash
# 打开命令提示符或 PowerShell
cd D:\code\v21\MaxKB

# 如果使用虚拟环境，先激活
venv\Scripts\activate

# 启动 Web 服务（开发模式）
python main.py dev web
```

#### 方式3：启动 Celery 任务队列

```bash
# 新开一个终端窗口
cd D:\code\v21\MaxKB
venv\Scripts\activate

# 启动 Celery
python main.py dev celery
```

#### 方式4：直接使用 Django 命令

```bash
# Web 服务
cd D:\code\v21\MaxKB\apps
python manage.py runserver 0.0.0.0:8080

# Celery（新开终端）
cd D:\code\v21\MaxKB\apps
python manage.py celery celery
```

---

### ❌ 不要在 Windows 上使用这些命令

```bash
# ❌ 错误：会尝试启动 Gunicorn（不支持 Windows）
python main.py start web
python main.py start all
python main.py start task

# ❌ 错误：daemon 模式不支持 Windows
python main.py start web -d
python main.py start all --daemon
```

---

### 🔧 已修复的兼容性问题

#### 1. `No module named 'pwd'` 错误
- **原因**：`daemon` 模块依赖 Unix 特定的 `pwd` 模块
- **修复**：在 `apps/common/management/commands/services/utils.py` 中添加了平台检测，Windows 上不导入 `daemon`

#### 2. `module 'os' has no attribute 'getuid'` 错误
- **原因**：`os.getuid()` 是 Unix 特定函数
- **修复**：在以下文件中添加了 `hasattr(os, 'getuid')` 检查：
  - `apps/common/management/commands/services/services/celery_base.py`
  - `apps/common/management/commands/services/services/celery_default.py`
  - `apps/common/management/commands/celery.py`

#### 3. 临时目录路径问题
- **原因**：硬编码了 Linux 路径 `/opt/maxkb-app/tmp/`
- **修复**：在 `main.py` 中自动创建 Windows 兼容的临时目录

---

### 🐛 Windows 常见问题

#### Q: 为什么不能使用 `python main.py start web`？
A: `start` 命令会尝试启动 Gunicorn 服务器，Gunicorn 仅支持 Unix/Linux 系统。Windows 上必须使用 `dev` 命令。

#### Q: 生产环境怎么办？
A: Windows 不适合作为 MaxKB 的生产环境。建议：
- 使用 Linux 服务器部署生产环境
- 或使用 Docker 容器部署
- Windows 仅用于开发和测试

#### Q: Celery 是必须的吗？
A: 不是必须的。如果只是测试基本功能，只启动 Web 服务即可。Celery 用于处理异步任务（如文档向量化、定时任务等）。

---

### 📝 技术说明

#### 为什么 Gunicorn 不支持 Windows？

Gunicorn 使用了 Unix 特定的功能：
- `fork()` 系统调用（Windows 不支持）
- Unix 信号处理机制
- Unix 套接字和进程管理

#### Windows 上的替代方案

**开发环境**：
- ✅ Django `runserver`（已集成在 `dev` 命令中）

**生产环境**（如果必须用 Windows）：
- 使用 `waitress`（纯 Python WSGI 服务器，支持 Windows）
- 使用 IIS + wfastcgi
- **推荐**：使用 Docker Desktop + Linux 容器

---

## 5. 前端启动

### 5.1 安装Node.js依赖

```bash
# 进入前端目录
cd D:/code/v21/MaxKB/ui

# 安装依赖（使用npm或pnpm）
npm install
# 或
pnpm install
```

### 5.2 启动前端开发服务器

```bash
# 启动管理后台（默认端口3000）
npm run dev

# 或启动聊天界面（端口3001）
npm run chat
```

**前端配置说明**:
- 管理后台配置: `ui/env/.env` (如不存在需创建)
- 聊天界面配置: `ui/env/.env.chat`

创建 `ui/env/.env` 文件：
```bash
VITE_APP_NAME=admin
VITE_BASE_PATH=/admin/
VITE_APP_PORT=3000
VITE_APP_TITLE='MaxKB'
VITE_ENTRY="admin.html"
```

### 5.3 访问前端

- **管理后台**: http://localhost:3000/admin/
- **聊天界面**: http://localhost:3001/chat/

**注意**: 前端会自动代理API请求到后端 `http://127.0.0.1:8080`（配置在 `ui/vite.config.ts`）

---

## 6. 常见问题

### 6.1 数据库连接失败

**错误**: `could not connect to server: Connection refused`

**解决**:
```bash
# 检查PostgreSQL是否运行
# Windows
services.msc  # 查找PostgreSQL服务

# 检查端口占用
netstat -ano | findstr :5432

# 测试连接
psql -U root -h 127.0.0.1 -d maxkb
```

---

### 6.2 pgvector扩展未安装

**错误**: `extension "vector" does not exist`

**解决**:
```sql
-- 连接到maxkb数据库
\c maxkb

-- 创建扩展
CREATE EXTENSION vector;

-- 如果提示权限不足，使用超级用户
CREATE EXTENSION IF NOT EXISTS vector;
```

---

### 6.3 Redis连接失败

**错误**: `Error 10061 connecting to 127.0.0.1:6379`

**解决**:
```bash
# 检查Redis是否运行
redis-cli ping

# 如果使用Docker
docker ps | grep redis
docker start maxkb-redis

# 检查密码配置
redis-cli -h 127.0.0.1 -p 6379 -a Password123@redis
```

---

### 6.4 Python依赖安装失败

**错误**: `torch` 或其他包安装失败

**解决**:
```bash
# 使用国内镜像源
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r pyproject.toml

# 或单独安装torch（CPU版本）
pip install torch --index-url https://download.pytorch.org/whl/cpu

# Windows可能需要安装Visual C++构建工具
https://visualstudio.microsoft.com/visual-cpp-build-tools/
```

---

### 6.5 前端启动失败

**错误**: `Cannot find module 'vite'`

**解决**:
```bash
# 清除缓存重新安装
cd ui
rm -rf node_modules package-lock.json
npm install

# 或使用pnpm
pnpm install
```

---

### 6.6 端口被占用

**错误**: `Address already in use: 8080`

**解决**:
```bash
# Windows查找占用进程
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F

# 或修改端口
python main.py dev web  # 默认8080
# 修改 apps/maxkb/settings/base.py 中的端口配置
```

---

### 6.7 Docker Desktop未启动

**错误**: `error during connect: This error may indicate that the docker daemon is not running`

**解决**: 
1. 启动Docker Desktop
2. 等待Docker完全启动（托盘图标不再转动）
3. 重新运行脚本

---

### 6.8 端口被占用

**错误**: `Bind for 0.0.0.0:5432 failed: port is already allocated`

**解决**:
```bash
# 查找占用端口的进程
netstat -ano | findstr :5432

# 停止占用的进程
taskkill /PID <进程ID> /F

# 或修改.env使用其他端口
MAXKB_DB_PORT=5433
```

---

### 6.9 容器已存在

**错误**: `The container name "/maxkb-postgres" is already in use`

**解决**:
```bash
# 方案1: 启动现有容器
docker start maxkb-postgres

# 方案2: 删除旧容器重新创建
docker rm -f maxkb-postgres
# 然后重新运行启动脚本
```

---

### 6.10 数据库迁移失败

**错误**: `django.db.utils.OperationalError: could not connect to server`

**解决**:
```bash
# 1. 检查容器是否运行
docker ps | findstr maxkb-postgres

# 2. 检查容器日志
docker logs maxkb-postgres

# 3. 等待几秒后重试
timeout /t 10
python main.py upgrade_db
```

---

## 7. 开发工具推荐

### 7.1 VSCode插件

- **Python** - Python语言支持
- **Pylance** - Python智能提示
- **Vue - Official** - Vue 3支持
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

### 7.2 调试配置

创建 `.vscode/launch.json`:
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Django",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "args": ["dev", "web"],
      "django": true,
      "justMyCode": false
    }
  ]
}
```

---

## 8. 项目结构说明

```
MaxKB/
├── apps/                    # 后端应用
│   ├── application/        # 应用管理
│   ├── knowledge/          # 知识库
│   ├── models_provider/    # 模型提供商
│   ├── maxkb/             # 核心配置
│   └── manage.py          # Django管理脚本
├── ui/                     # 前端应用
│   ├── src/               # 源代码
│   ├── public/            # 静态资源
│   └── package.json       # 依赖配置
├── installer/             # Docker部署脚本
├── main.py               # 主启动脚本
└── pyproject.toml        # Python依赖配置
```

---

## 🔍 验证安装

### 检查pgvector扩展

```bash
# 连接到数据库
psql -h 127.0.0.1 -p 5432 -U root -d maxkb

# 检查扩展
SELECT * FROM pg_extension WHERE extname = 'vector';

# 应该看到类似输出：
#  extname | extowner | extnamespace | extrelocatable | extversion 
# ---------+----------+--------------+----------------+------------
#  vector  |       10 |         2200 | t              | 0.7.0
```

### 检查服务状态

```bash
# 检查Docker容器
docker ps

# 应该看到两个容器运行：
# maxkb-postgres
# maxkb-redis
```

### 访问应用

- **后端API**: http://localhost:8080/admin/api/docs
- **管理后台**: http://localhost:8080/admin/
- **默认账号**: admin / MaxKB@123..

---

## 🛠️ 常用命令

### Docker容器管理

```bash
# 查看运行中的容器
docker ps

# 查看所有容器（包括停止的）
docker ps -a

# 启动容器
docker start maxkb-postgres maxkb-redis

# 停止容器
docker stop maxkb-postgres maxkb-redis

# 重启容器
docker restart maxkb-postgres maxkb-redis

# 查看容器日志
docker logs maxkb-postgres
docker logs maxkb-redis

# 进入容器
docker exec -it maxkb-postgres bash
docker exec -it maxkb-redis bash

# 删除容器（会丢失数据！）
docker rm -f maxkb-postgres maxkb-redis
```

### 数据库管理

```bash
# 连接PostgreSQL
psql -h 127.0.0.1 -p 5432 -U root -d maxkb

# 常用SQL命令
\l              # 列出所有数据库
\dt             # 列出所有表
\d+ table_name  # 查看表结构
\q              # 退出

# 备份数据库
docker exec maxkb-postgres pg_dump -U root maxkb > backup.sql

# 恢复数据库
docker exec -i maxkb-postgres psql -U root maxkb < backup.sql
```

---

## 🔄 日常启动流程

```bash
# 终端1: 后端
cd D:/code/v21/MaxKB
venv\Scripts\activate
python main.py dev web

# 终端2: 前端
cd D:/code/v21/MaxKB/ui
npm run dev

# 终端3: Celery（可选）
cd D:/code/v21/MaxKB/apps
python manage.py celery celery
```

---

## 📊 资源使用

### Docker容器资源占用

- **PostgreSQL**: ~200MB 内存
- **Redis**: ~10MB 内存
- **总磁盘**: ~500MB

### 清理资源

```bash
# 停止并删除容器
docker stop maxkb-postgres maxkb-redis
docker rm maxkb-postgres maxkb-redis

# 删除数据卷（会丢失所有数据！）
docker volume prune

# 删除镜像
docker rmi pgvector/pgvector:pg17 redis:latest
```

---

## 🔄 切换到远程服务器

如果远程服务器安装了pgvector，可以切换回去：

### 步骤1: 修改 `.env` 文件

注释掉本地配置，启用远程配置：

```bash
# 本地配置（注释掉）
# MAXKB_DB_HOST=127.0.0.1
# MAXKB_DB_PORT=5432

# 远程配置（启用）
MAXKB_DB_HOST=10.80.17.190
MAXKB_DB_PORT=5433
```

### 步骤2: 停止本地容器

```bash
docker stop maxkb-postgres maxkb-redis
```

### 步骤3: 重新启动

```bash
python main.py dev web
```

---

## 📝 下一步

1. ✅ 环境启动成功
2. 📖 查看 [API文档](http://localhost:8080/admin/api/docs)
3. 🎨 启动前端开发服务器
4. 🚀 开始开发

---

## 🔗 相关文件

- **启动脚本**: `start_local_dev.bat`
- **停止脚本**: `stop_local_dev.bat`
- **环境配置**: `.env`
- **Windows兼容性**: `Windows兼容性修复说明.md`
- **RAG优化**: `MaxKB_RAG优化使用指南.md`

---

**最后更新**: 2026-01-20  
**适用版本**: MaxKB v2.0.0

**祝开发顺利！** 🚀
