/** 「知识库」各场景按钮权限：$perm.knowledge.workspace.* / .system.* / .share.* */

import workspace from './workspace'
import system from './system'
import share from './share'

export default { workspace, system, share }
