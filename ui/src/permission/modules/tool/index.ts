/** 「工具」各场景按钮权限：$perm.tool.workspace.* / .system.* / .share.* */

import workspace from './workspace'
import system from './system'
import share from './share'

export default { workspace,
  /**
   * 系统「工具」权限
   * */
  system,
  /**系统「共享工具」权限
   * */
  share }
