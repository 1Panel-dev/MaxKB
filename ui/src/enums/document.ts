export enum hitHandlingMethod {
  optimization = '模型优化',
  directly_return = '直接回答'
}

export enum hitStatus {
  waiting = '等待中',
  processing = '处理中',
  completed = '已完成',
  failed = '失败'
}

export enum isActivated {
  true = '启用',
  false = '禁用'
}
