# System API

该目录按系统管理业务拆分 Admin API，例如用户、角色、工作空间、认证设置和外观设置。
每个业务使用独立文件，目录结构如下：

```text
system/
├── README.md
├── user-manage.ts
└── workspace.ts
```

- 每个接口单独导出，并在文件末尾提供默认 API 对象。
- 新增系统业务时直接创建同名 `.ts` 文件，不建立业务文件夹。
- 所有请求和响应类型统一在 `src/types` 声明，并从 `@/types` 导入。
- 系统级资源与工作空间级同名资源分别维护，不跨业务域合并。
